# 📤 GitHub 推送操作指南

## 🎯 快速开始

### 方式一：自动触发构建（推荐）

直接推送代码即可自动触发 GitHub Actions 构建：

```bash
cd /DouyinTV
git push origin main
```

### 方式二：手动触发构建

如果不需要推送代码，可以通过 GitHub 网页手动触发：

1. 访问仓库：https://github.com/bitstreamcn/DouyinTV
2. 点击 **Actions** 标签
3. 选择 `Build DouyinTV APK` 工作流
4. 点击 **Run workflow** 按钮
5. 选择分支 `main`
6. 点击 **Run workflow** 确认

---

## 🚀 推送成功后

### 1️⃣ 查看自动构建

1. 访问：https://github.com/bitstreamcn/DouyinTV/actions
2. 选择最近的构建任务
3. 等待构建完成（约5-10分钟）

### 2️⃣ 下载 APK

构建完成后：

1. 在 Actions 页面，点击具体的构建任务
2. 滚动到页面底部的 **Artifacts** 部分
3. 下载 `DouyinTV-Debug-APK`
4. 解压后获得 `app-debug.apk`

### 3️⃣ 安装到电视

**方法一：通过USB传输**
```bash
# 1. 将 APK 文件复制到USB
# 2. 将USB插到电视
# 3. 在电视上找到APK文件并安装
```

**方法二：通过 ADB 安装**
```bash
# 确保电视和电脑在同一网络
adb connect <电视IP>:5555
adb install app-debug.apk
```

**方法三：通过局域网传输**
1. 将 APK 上传到网盘或文件服务器（如百度网盘、坚果云）
2. 在电视上使用网络浏览器或第三方应用下载安装

---

## 📋 项目结构

```
DouyinTV/
├── .github/
│   └── workflows/
│       └── build-apk.yml           # GitHub Actions 配置
├── app/
│   ├── build.gradle.kts             # App 构建配置
│   ├── build-release.sh             # Release 构建脚本
│   └── src/main/
│       ├── AndroidManifest.xml      # 应用清单文件
│       ├── java/com/douyin/tv/
│       │   ├── MainActivity.kt      # 主界面
│       │   ├── VideoPlayerActivity.kt  # 视频播放器
│       │   ├── WebDialog.kt         # WebView 对话框
│       │   ├── TVMainScreen.kt      # 主界面 UI
│       │   ├── DefaultRenderersFactoryProvider.kt
│       │   ├── DouyinTVApplication.kt
│       │   └── GlobalContext.kt
│       └── res/
│           ├── drawable/
│           │   └── app_banner.xml   # TV 横幅图标
│           ├── mipmap/
│           │   ├── ic_launcher.xml  # 应用图标
│           │   └── ic_launcher_round.xml
│           └── values/
│               ├── colors.xml
│               ├── strings.xml
│               └── themes.xml
├── build.gradle.kts                 # 项目构建配置
├── settings.gradle.kts              # Gradle 设置
├── gradle.properties                # Gradle 属性
└── README.md                        # 项目说明文档
```

---

## 🔧 常见问题

### Q1: 推送时提示认证失败
```bash
A: 检查 ~/.netrc 文件配置是否正确
```

### Q2: Actions 构建失败
```bash
A: 检查 .github/workflows/build-apk.yml 配置
A: 查看构建日志中的错误信息
```

### Q3: APK 无法安装
```bash
A: 确保电视已开启"允许安装未知来源应用"选项
A: 检查 APK 版本与系统兼容性
```

### Q4: 应用无法播放视频
```bash
A: 检查网络连接
A: 查看 Logcat 日志: adb logcat | grep DouyinTV
```

### Q5: 如何查看应用日志
```bash
# 连接电视后
adb logcat | grep -E "DouyinTV|VideoPlayer"
```

---

## 📬 技术支持

如有问题，请查看：
- GitHub Actions 文档：https://docs.github.com/en/actions
- Android 开发文档：https://developer.android.com/docs
- Media3 文档：https://developer.android.com/guide/topics/media/media3
- 项目 README.md：README.md

---

## 📝 项目特点

- ✅ Kotlin 语言开发
- ✅ Media3 视频播放器（ExoPlayer 继任者）
- ✅ Jetpack Compose UI 框架
- ✅ WebView 加载抖音网页
- ✅ 自动拦截视频 URL
- ✅ Android 6.0 (API 23) 兼容
- ✅ 遥控器操作支持
- ✅ GitHub Actions 自动构建
- ✅ 容器化构建环境

---

## 🎮 遥控器按键说明

| 按键 | 功能 |
|------|------|
| 方向键 | 导航 |
| 确认键 | 选择/播放/暂停 |
| 返回键 | 返回上一级 |
| 播放键 | 播放视频 |
| 暂停键 | 暂停视频 |
| 快进 | 快进 10 秒 |
| 快退 | 快退 10 秒 |

---

*更新时间：2026-02-23*
